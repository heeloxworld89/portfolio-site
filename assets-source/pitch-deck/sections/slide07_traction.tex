\begin{frame}{The Evidence Is Open. Replay a Real Run in Your Browser.}
\actionheadline{Checkable Without Us in the Room --- and One of Them Is Running Live Right Now}

{\fontsize{7.0}{8.6}\selectfont
\begin{columns}[T]
  \column{0.49\textwidth}
  \widecolcard[3.2cm]{LIVE \; \amb{oxiedo.com/black-box}}{%
    An archived training run, replayable epoch by epoch, two identical networks side by side.\par\vspace{3pt}
    \amb{$\bullet$} Step through epochs 0--199 and watch per-component health move before the loss curve does.\par\vspace{2pt}
    \amb{$\bullet$} Open the correction ledger: step, component, diagnosis, magnitude, declared ceiling.\par\vspace{2pt}
    \amb{$\bullet$} Four scenarios in the selector, including the one where this architecture loses.\par\vspace{3pt}
    \textbf{Seeds on screen. Every run regenerates on a customer's own hardware.}}
  \column{0.49\textwidth}
  \widecolcard[3.2cm]{MEASURED \; 383 CONTROLLED EXPERIMENTS}{%
    Four architecture families, 67 archived runs, every one reproducible from seed.\par\vspace{3pt}
    \amb{Structural collapse} \pos{80.3\%} recovery where a parameter-matched baseline sits at \oxneg{10.0\%} permanently, on every seed.\par\vspace{2pt}
    \amb{Task shift} \pos{94.6\%} retained against \oxneg{47.3\%}, no replay buffer, no task ID.\par\vspace{2pt}
    \amb{Zero-shot composition} \pos{58.8\%} on unseen combinations against 25\% chance. Not designed. It emerged.\par\vspace{2pt}
    \amb{Adversarial injection} \oxneg{$-$1.0 pp} against baseline. The one adverse result, published alongside the rest.}
\end{columns}
}

\footnotedata{All results CIFAR-10 and CIFAR-100. No clinical, financial, biological or defence data has touched this system, and transformers are a stated target rather than a demonstrated result. Both are named on every page where a result appears.}
\end{frame}
